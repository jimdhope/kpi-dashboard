# IDE & AI tools
.opencode/
.cursor/
.vscode/
.idx/
.modified

# Build artifacts
.next
node_modules
tsconfig.tsbuildinfo
tsconfig.typecheck.tsbuildinfo
coverage
dist

# Environment
.env
.env.local
.env.*.local

# Local scripts are ignored except for the production authentication cutover
# utility, which is bundled into the release image.
scripts/*
!scripts/auth-cutover.ts
perf/
check_*.ts
debug_*.ts

# Development tools
badge-preview-server/

# macOS
.DS_Store

# Personal config
config.json
cookies.txt

# Local version copies (app is at root level)
v2/
v3/
v3.1/
