"use client";
import { useEffect,useState } from "react";
import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card";
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
type Item={id:string;subject:string;message:string;status:string;createdAt:string;user?:{name:string;email:string}|null;attachments?:{originalName:string;storedName:string;size:number}[]};
export default function FeedbackTriagePage(){const [items,setItems]=useState<Item[]>([]);const load=()=>fetch('/api/feedback?all=true').then(r=>r.json()).then(d=>setItems(d.feedback||[]));useEffect(()=>{load()},[]);const update=(id:string,status:string)=>fetch(`/api/feedback/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})}).then(load);return <div className="mx-auto max-w-3xl space-y-6"><div><h1 className="text-3xl font-bold">Feedback triage</h1><p className="text-muted-foreground">Review feedback and track progress.</p></div><Card><CardContent className="space-y-3 pt-6">{items.length===0?<p className="text-muted-foreground">No feedback submitted.</p>:items.map(i=><div className="rounded border p-3" key={i.id}><div className="flex justify-between gap-3"><div><b>{i.subject}</b><p className="text-xs text-muted-foreground">{i.user?.name||'Unknown'} · {new Date(i.createdAt).toLocaleString()}</p></div><Select value={i.status} onValueChange={v=>update(i.id,v)}><SelectTrigger className="w-36"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="NEW">New</SelectItem><SelectItem value="IN_PROGRESS">In progress</SelectItem><SelectItem value="RESOLVED">Resolved</SelectItem></SelectContent></Select></div><p className="mt-2 whitespace-pre-wrap text-sm">{i.message}</p>{i.attachments?.length?<div className="mt-2 flex flex-wrap gap-2">{i.attachments.map(a=><a key={a.storedName} className="text-xs text-primary underline underline-offset-2" href={`/api/feedback/${i.id}/attachments/${encodeURIComponent(a.storedName)}`}>{a.originalName} ({Math.ceil(a.size/1024)} KB)</a>)}</div>:null}</div>)}</CardContent></Card></div>}
